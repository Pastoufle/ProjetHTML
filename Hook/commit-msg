# Définir le message initial
commit_message="Votre message de commit existant ici."

# Ajouter du texte supplémentaire
additional_text="Ajout de texte supplémentaire."

# Concaténer les messages
final_commit_message="$commit_message $additional_text"

# Effectuer le commit avec le message final
git commit -m "$final_commit_message"
